---
description: Dart style — early return, no var, for not forEach, no then, dot shortcuts
globs: "**/*.dart"
alwaysApply: false
---

## 所有方法必须使用 early return

Bad 😡

```dart
void foo() {
  if (condition) {
    // do something
  }
}
```

Good ✅

```dart
void foo() {
  if (!condition) {
    return;
  }
  // do something
}
```

## 禁止使用 `var` 声明变量，使用明确类型 + `final`

- 不用 `var`，用**明确类型**和 **`final`** 声明变量（需要 reassign 时再用非 final 的显式类型）。
- 提高可读性和类型清晰度，便于协作与维护。

```dart
// Bad 😡
var name = 'foo';
var count = 0;
var list = <int>[];

// Good ✅
final String name = 'foo';
final int count = 0;
final List<int> list = <int>[];
```

`for` 循环中的迭代变量用 `final`：

```dart
for (final e in list) {
  // ...
}
```

## 使用 for 循环, 禁止使用 forEach 函数

```dart
List<T> list;
// Bad 😡
list.forEach((e) {
  // ...
});

// Good ✅
for (final e in list) {
  // ...
}
```

## 不要使用 `then`

使用 `then` 会降低代码的可读性

```dart
Future<void> foo() async {
  await Future.delayed(const Duration(seconds: 1));
}

// Bad 😡
foo().then((value) {
  // ...
});

// Good ✅
final value = await foo();
```

## Dot shortcuts

参考下面的 dot shorthands 优化你写的 flutter / dart 代码

```dart
// Use dot shorthand syntax on enums:
enum Status { none, running, stopped, paused }

Status currentStatus = .running; // Instead of Status.running

// Use dot shorthand syntax on a static method:
int port = .parse('8080'); // Instead of int.parse('8080')

// Uses dot shorthand syntax on a constructor:
class Point {
  final int x, y;
  Point(this.x, this.y);
  Point.origin() : x = 0, y = 0;
}

Point origin = .origin(); // Instead of Point.origin()
```

## 已知的可以用 dot shorthands 的 Symbols

```dart
Alignment
BoxShape
CrossAxisAlignment
EdgeInsets
EdgeInsets
FontWeight
MainAxisAlignment
MainAxisSize
Radius
TextScaler
```
