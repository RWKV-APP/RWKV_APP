# local_web_search example

Desktop debug app for the `local_web_search` package.

```bash
flutter run -d macos
```

The app loads a browser panel, accepts a query or URL, and can execute the Google SERP extraction script against the current page.
