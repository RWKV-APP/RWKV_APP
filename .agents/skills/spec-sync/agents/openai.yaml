interface:
  display_name: "Spec Sync"
  short_description: "Synchronize product truth, conflicts, and acceptance"
  default_prompt: "Use $spec-sync to keep raw input in the private Root Harness and synchronize only normalized project truth before implementation."
